import React, { Component } from 'react';
import './App.css';

class DeleteButton extends Component {
    render() {
        return (
            <button className="Nav">Delete</button>

        );
    }
}

export default DeleteButton;