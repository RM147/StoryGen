import React, { Component } from 'react';
import './App.css';

class ReadButton extends Component {
    render() {
        return (
            <button className="Nav">Read</button>

        );
    }
}

export default ReadButton;