import React, { Component } from 'react';
import './App.css';

class UpdateButton extends Component {
    render() {
        return (
            <button className="Nav">Update</button>

        );
    }
}

export default UpdateButton;